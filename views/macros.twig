{% macro input(name, value, type, size, placeholder) %}
    <input type="{{ type|default('text') }}" name="{{ name }}" value="{{ value|e }}" placeholder="{{ placeholder|default('') }}" size="{{ size|default(20) }}" />
{% endmacro %}